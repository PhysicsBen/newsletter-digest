[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python -u -m src.pipeline"
# ON_FAILURE: restart only on non-zero exit; clean pipeline exits (0) stop the container.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
