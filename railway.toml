[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "alembic upgrade head && python -m src.pipeline"
# ON_FAILURE: restart only on non-zero exit; clean pipeline exits (0) stop the container.
# This prevents the infinite restart loop that swallows logs.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
